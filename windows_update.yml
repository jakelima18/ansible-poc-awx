---
- name: Windows Updates
  hosts: all
  gather_facts: false
  tasks:
    - name: Install Security/Critical updates (reboot if needed)
      ansible.windows.win_updates:
        category_names:
          - SecurityUpdates
          - CriticalUpdates
          - UpdateRollups
        state: installed
        reboot: true
        reboot_timeout: 3600
